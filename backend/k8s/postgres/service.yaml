apiVersion: v1
kind: Service
metadata:
  name: postgres-inventory
  namespace: ecommerce
spec:
  selector:
    app: postgres-inventory
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP